<!DOCTYPE html>
<html lang="he">
<head>
<title>Search</title>


</style>
<meta charset="UTF-8">

    <link rel="stylesheet" href="searching.css">
</head>

<body>
<div class="top-navbar">
    <div class="logo">SmartShop</div>

    <div class="nav-links">
        <a href="Home.html">Home</a>
        <a href="#">Search</a>
        <a href="#">Filters</a>
        <a href="#">Profile</a>
    </div>
</div>

</div>
<div class="search-label">✨ Discover something new</div>


<div class="dropdown">
    
    <div class="search-box">
        
    <input 
        type="text" 
        class="dropdown-search" 
        placeholder="Search product type..."
    
    >
    <span class="orb-blue"></span>
<span class="orb-red"></span>
<span class="orb-green"></span>
<span class="orb-yellow"></span>

 
    <span class="clear-btn">×</span>
</div>

    <div class="dropdown-list" id="dropdownList">
        <div class="dropdown-item">Phones</div>  
        <div class="dropdown-item">Cars</div>  
        <div class="dropdown-item">Furniture</div>  
        <div class="dropdown-item">Gaming</div>  
        <div class="dropdown-item">Clothing</div>  
        <div class="dropdown-item">Books</div>  
        <div class="dropdown-item">Sports</div>  
        <div class="dropdown-item">Toys</div>  
        <div class="dropdown-item">Music</div>  
        <div class="dropdown-item">Appliances</div>  
        <div class="dropdown-item">Jewelry</div>  
        <div class="dropdown-item">Beauty</div>  
        <div class="dropdown-item">Shoes</div>  
        <div class="dropdown-item">Bags</div>  
        <div class="dropdown-item">Watches</div>  
        <div class="dropdown-item">Garden</div>  
        <div class="dropdown-item">Pets</div>  
        <div class="dropdown-item">Tools</div>  
        <div class="dropdown-item">Art</div>  
        <div class="dropdown-item">Office</div>
   

    </div>
  
</div>
<div class="describe-box">
    <textarea placeholder="Describe the product..."></textarea>
</div>

<div id="subDropdown"></div>

<div class="categories">
    <div class="category" id="cat1" data-category="Phones"><div class="circle"><img src="img1.jpg"></div></div>
    <div class="category" id="cat2" data-category="Cars"><div class="circle"><img src="img2.jpg"></div></div>
    <div class="category" id="cat3" data-category="Furniture"><div class="circle"><img src="img3.jpg"></div></div>
    <div class="category" id="cat4" data-category="Gaming"><div class="circle"><img src="img4.jpg"></div></div>
    <div class="category" id="cat5" data-category="Clothing"><div class="circle"><img src="img5.jpg"></div></div>
    <div class="category" id="cat6" data-category="Books"><div class="circle"><img src="img6.jpg"></div></div>
    <div class="category" id="cat7" data-category="Sports"><div class="circle"><img src="img7.jpg"></div></div>
    <div class="category" id="cat8" data-category="Toys"><div class="circle"><img src="img8.jpg"></div></div>
    <div class="category" id="cat9" data-category="Music"><div class="circle"><img src="img9.jpg"></div></div>
    <div class="category" id="cat10" data-category="Appliances"><div class="circle"><img src="img10.jpg"></div></div>
    <div class="category" id="cat11" data-category="Jewelry"><div class="circle"><img src="img11.jpg"></div></div>
    <div class="category" id="cat12" data-category="Beauty"><div class="circle"><img src="img12.jpg"></div></div>
    <div class="category" id="cat13" data-category="Shoes"><div class="circle"><img src="img13.jpg"></div></div>
    <div class="category" id="cat14" data-category="Bags"><div class="circle"><img src="img14.jpg"></div></div>
    <div class="category" id="cat15" data-category="Watches"><div class="circle"><img src="img15.jpg"></div></div>
    <div class="category" id="cat16" data-category="Garden"><div class="circle"><img src="img16.jpg"></div></div>
    <div class="category" id="cat17" data-category="Pets"><div class="circle"><img src="img17.jpg"></div></div>
    <div class="category" id="cat18" data-category="Tools"><div class="circle"><img src="img18.jpg"></div></div>
    <div class="category" id="cat19" data-category="Art"><div class="circle"><img src="img19.jpg"></div></div>
    <div class="category" id="cat20" data-category="Office"><div class="circle"><img src="img20.jpg"></div></div>
</div>
<div class="bottom-footer">
    <a href="#">Search</a>
    <a href="#">Help</a>
    <a href="#">Terms</a>
    <a href="#">Contact</a>
</div>
<div id="floatingSearchBtn">Search</div>
<script src="search.js"></script>
</body>
</html>

css
/* ================= BODY ================= */
body{
    margin:0;
    min-height:100vh;

    font-family:
        Inter,
        "SF Pro Display",
        Arial,
        sans-serif;

    color:#1f2937;

    background:
        radial-gradient(
            circle at 12% 18%,
            rgba(255,215,0,.35),
            transparent 30%
        ),

        radial-gradient(
            circle at 88% 15%,
            rgba(255,230,160,.22),
            transparent 28%
        ),

        radial-gradient(
            circle at 22% 82%,
            rgba(255,193,7,.18),
            transparent 36%
        ),

        radial-gradient(
            circle at 82% 78%,
            rgba(66,133,244,.15),
            transparent 38%
        ),

        radial-gradient(
            circle at 55% 35%,
            rgba(120,190,255,.08),
            transparent 42%
        ),

        linear-gradient(
            135deg,
            #ffffff 0%,
            #fffdf8 20%,
            #faf7ef 45%,
            #f7f9fc 75%,
            #eef4fb 100%
        );

    background-attachment:fixed;
}

/* ================= NAVBAR ================= */
.top-navbar {
    position: sticky;
    top: 0;
    z-index: 1000;

    display: flex;
    justify-content: space-between;
    align-items: center;

    padding: 14px 24px;

    background: rgba(255,255,255,0.55);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);

    border-bottom: 1px solid rgba(0,0,0,0.05);

    box-shadow:
        0 10px 35px rgba(0,0,0,0.07),
        0 1px 0 rgba(255,255,255,0.7) inset;

    transition: transform 0.25s ease, opacity 0.25s ease;
}

.top-navbar.scrolled {
    transform: translateY(-110%);
    opacity: 0;
    pointer-events: none;
}

.top-navbar::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
        90deg,
        rgba(0,114,255,0.06),
        rgba(0,198,255,0.04),
        transparent
    );
    pointer-events: none;
}

.top-navbar .logo {
    font-weight: 800;
    font-size: 15px;

    background: linear-gradient(90deg, #0072ff, #00c6ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;

    letter-spacing: 0.5px;
}

/* ================= NAV LINKS ================= */
.nav-links {
    display: flex;
    gap: 20px;
    align-items: center;
}

.nav-links a {
    font-size: 13px;
    color: #6b7280;
    text-decoration: none;

    position: relative;
    padding: 6px 4px;

    transition: all 0.25s ease;
}

.nav-links a::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -4px;

    width: 0%;
    height: 2px;
    border-radius: 999px;

    background: linear-gradient(90deg, #0072ff, #00c6ff);

    transition: width 0.3s ease;
}

.nav-links a:hover {
    color: #0072ff;
    transform: translateY(-1px);
}

.nav-links a:hover::after {
    width: 100%;
}

/* אפקט עדין של עומק בלינקים */
.nav-links a:active {
    transform: scale(0.96);
}

/* ================= TITLE ================= */
.search-label{
    position:;

    display:flex;
    justify-content:center;
    align-items:center;

    margin-top:55px;
    margin-bottom:35px;

    font-size:clamp(28px,4vw,48px);
    font-weight:200;

    letter-spacing:-1.2px;
    line-height:1.05;

    text-align:center;

    background:
    linear-gradient(
        90deg,
        #4285F4 0%,
        #5A9BFF 18%,
        #34A853 38%,
        #2E7D32 52%,
        #C97A1C 70%,
        #B45309 82%,
        #B23A2F 92%,
        #7A1F1A 100%
    );

    background-size:300% 100%;

    -webkit-background-clip:text;
    background-clip:text;
    -webkit-text-fill-color:transparent;

    filter:
        drop-shadow(0 0 12px rgba(66,133,244,.18))
        drop-shadow(0 0 20px rgba(52,168,83,.10));

    animation:
        googleTextFlow 10s linear infinite,
        googleFloat 6s ease-in-out infinite;
}

/* glass aura */
.search-label::before{
    content:"";

    position:absolute;

    width:80%;
    height:220%;

    left:50%;
    top:50%;

    transform:
        translate(-50%,-50%);

    border-radius:999px;

    background:
        radial-gradient(
            circle,
            rgba(66,133,244,.12),
            rgba(52,168,83,.08),
            rgba(251,188,5,.06),
            transparent 70%
        );

    filter:blur(45px);

    z-index:-2;

    animation:
        googleAura 8s ease-in-out infinite;
}

/* premium animated underline */
.search-label::after{
    content:"";

    position:absolute;

    left:50%;
    bottom:-16px;

    transform:
        translateX(-50%);

    width:180px;
    height:4px;

    border-radius:999px;

    background:
        linear-gradient(
            90deg,
            #4285F4,
            #34A853,
            #FBBC05,
            #EA4335,
            #4285F4
        );

    background-size:300% 100%;

    box-shadow:
        0 0 10px rgba(66,133,244,.25),
        0 0 20px rgba(52,168,83,.18),
        0 0 30px rgba(234,67,53,.12);

    animation:
        googleLineFlow 4s linear infinite;
}

/* top sparkle */
.search-label span{
    position:relative;
}

.search-label span::before{
    content:"✦";

    position:absolute;

    top:-18px;
    right:-22px;

    font-size:14px;

    background:
        linear-gradient(
            135deg,
            #4285F4,
            #34A853,
            #FBBC05,
            #EA4335
        );

    -webkit-background-clip:text;
    -webkit-text-fill-color:transparent;

    animation:
        sparklePulse 2.5s ease-in-out infinite;
}

@keyframes googleTextFlow{

    0%{
        background-position:0% 50%;
    }

    100%{
        background-position:300% 50%;
    }
}

@keyframes googleLineFlow{

    0%{
        background-position:0% 50%;
    }

    100%{
        background-position:300% 50%;
    }
}

@keyframes googleFloat{

    0%{
        transform:translateY(0px);
    }

    50%{
        transform:translateY(-2px);
    }

    100%{
        transform:translateY(0px);
    }
}

@keyframes googleAura{

    0%{
        opacity:.65;
        transform:
            translate(-50%,-50%)
            scale(1);
    }

    50%{
        opacity:1;
        transform:
            translate(-50%,-50%)
            scale(1.08);
    }

    100%{
        opacity:.65;
        transform:
            translate(-50%,-50%)
            scale(1);
    }
}

@keyframes sparklePulse{

    0%{
        opacity:.4;
        transform:
            scale(.8)
            rotate(0deg);
    }

    50%{
        opacity:1;
        transform:
            scale(1.25)
            rotate(25deg);
    }

    100%{
        opacity:.4;
        transform:
            scale(.8)
            rotate(0deg);
    }
}

/* ================= SEARCH ================= */
/* =========================================================
   APPLE / OPENAI LEVEL PREMIUM TEXTAREA
========================================================= */
@property --angle{
    syntax:"<angle>";
    inherits:false;
    initial-value:0deg;
}

@property --x{
    syntax:"<percentage>";
    inherits:false;
    initial-value:20%;
}

@property --y{
    syntax:"<percentage>";
    inherits:false;
    initial-value:30%;
}

@property --x2{
    syntax:"<percentage>";
    inherits:false;
    initial-value:80%;
}

@property --y2{
    syntax:"<percentage>";
    inherits:false;
    initial-value:70%;
}

*{
    box-sizing:border-box;
}

.describe-box{
    display:flex;
    justify-content:center;
    margin:42px 15px;
}

.describe-box textarea{
    width:92vw;
    max-width:760px;
    min-height:130px;

    padding:22px 24px;

    border:2px solid transparent;
    border-radius:30px;

    outline:none;
    resize:none;

    color:#111827;
    font-size:15px;
    line-height:1.7;
    font-weight:200;

    background:
        radial-gradient(
            circle at var(--x) var(--y),
            rgba(66,133,244,.18),
            transparent 22%
        ),

        radial-gradient(
            circle at var(--x2) var(--y2),
            rgba(52,168,83,.14),
            transparent 24%
        ),

        radial-gradient(
            circle at 80% 18%,
            rgba(251,188,5,.12),
            transparent 18%
        ),

        radial-gradient(
            circle at 20% 85%,
            rgba(234,67,53,.10),
            transparent 20%
        ),

        linear-gradient(
            rgba(255,255,255,.92),
            rgba(255,255,255,.88)
        ) padding-box,

        conic-gradient(
            from var(--angle),
            #4285f4,
            #64a3ff,
            #34a853,
            #72db78,
            #fbbc05,
            #ffe08a,
            #ea4335,
            #ff8d86,
            #4285f4
        ) border-box;

    background-origin:border-box;

    background-clip:
        border-box,
        border-box,
        border-box,
        border-box,
        padding-box,
        border-box;

    backdrop-filter:blur(24px) saturate(170%);
    -webkit-backdrop-filter:blur(24px) saturate(170%);

    box-shadow:
        0 30px 80px rgba(15,23,42,.10),
        0 15px 35px rgba(15,23,42,.06),
        inset 0 1px 0 rgba(255,255,255,.95),
        inset 0 -1px 0 rgba(255,255,255,.40),
        0 0 0 1px rgba(255,255,255,.65);

    transition:
        transform .45s cubic-bezier(.22,1,.36,1),
        box-shadow .45s cubic-bezier(.22,1,.36,1),
        background .45s ease;

    animation:
        rotateBorder 4s linear infinite,
        liquidMove 18s ease-in-out infinite,
        breathingGlow 6s ease-in-out infinite;
}

.describe-box textarea::placeholder{
    color:rgba(55,65,81,.55);

    font-family:
        "Satoshi",
        "Manrope",
        "Inter",
        "SF Pro Display",
        "Segoe UI",
        sans-serif;

    font-size:17px;
    font-weight:300;
    letter-spacing:.2px;
}

.describe-box textarea:hover{
    transform:translateY(-3px);

    box-shadow:
        0 40px 90px rgba(66,133,244,.12),
        0 18px 40px rgba(15,23,42,.08),
        inset 0 1px 0 rgba(255,255,255,.95),
        0 0 40px rgba(66,133,244,.08);
}

.describe-box textarea:focus{
    transform:
        translateY(-2px)
        scale(1.005);

    background:
        radial-gradient(
            circle at var(--x) var(--y),
            rgba(66,133,244,.18),
            transparent 24%
        ),

        radial-gradient(
            circle at var(--x2) var(--y2),
            rgba(52,168,83,.14),
            transparent 25%
        ),

        radial-gradient(
            circle at 80% 18%,
            rgba(251,188,5,.12),
            transparent 18%
        ),

        radial-gradient(
            circle at 20% 85%,
            rgba(234,67,53,.10),
            transparent 20%
        ),

        linear-gradient(
            rgba(255,255,255,.95),
            rgba(255,255,255,.92)
        ) padding-box,

        conic-gradient(
            from var(--angle),
            #4285f4,
            #6faeff,
            #34a853,
            #7fe184,
            #fbbc05,
            #ffe27c,
            #ea4335,
            #ff9a90,
            #4285f4
        ) border-box;

    background-origin:border-box;

    background-clip:
        border-box,
        border-box,
        border-box,
        border-box,
        padding-box,
        border-box;

    box-shadow:
        0 20px 50px rgba(15,23,42,.08),
        0 10px 25px rgba(15,23,42,.05),
        0 0 0 3px rgba(66,133,244,.06),
        0 0 20px rgba(66,133,244,.08),
        inset 0 1px 0 rgba(255,255,255,.95);

    animation:
        rotateBorder 4s linear infinite,
        liquidMove 14s ease-in-out infinite,
        breathingGlow 5s ease-in-out infinite;
}

@keyframes rotateBorder{
    from{
        --angle:0deg;
    }
    to{
        --angle:360deg;
    }
}

@keyframes breathingGlow{
    50%{
        filter:
            brightness(1.08)
            saturate(1.15);
    }
}

@keyframes liquidMove{

    0%,100%{
        --x:18%;
        --y:25%;

        --x2:82%;
        --y2:70%;
    }

    25%{
        --x:75%;
        --y:18%;

        --x2:28%;
        --y2:80%;
    }

    50%{
        --x:88%;
        --y:70%;

        --x2:18%;
        --y2:30%;
    }

    75%{
        --x:35%;
        --y:88%;

        --x2:82%;
        --y2:22%;
    }
}

.dropdown{
    width:60%;
    margin:auto;
    position:relative;
    transition: width .35s ease;
    flex:none;
}

.dropdown.shrink{
    width:45%;
}

.search-box{
    position:relative;
    border-radius:34px;
}

.search-box::before{
    content:"";
    position:absolute;
    inset:0;
    border-radius:34px;
    background:
        radial-gradient(circle at 20% 30%,rgba(255,210,120,.35),transparent 30%),
        radial-gradient(circle at 80% 70%,rgba(255,230,180,.4),transparent 35%),
        linear-gradient(135deg,#fff,#f8f8f8);
    z-index:0;
}

.search-box::after{
    content:"";
    position:absolute;
    inset:-2px;
    border-radius:36px;
    background:
        conic-gradient(
            from var(--p),
            rgba(255,220,150,.7),
            rgba(255,255,255,.9),
            rgba(255,195,90,.7),
            rgba(255,255,255,.9),
            rgba(255,220,150,.7)
        );
    filter:blur(12px);
    opacity:.8;
    z-index:-1;
    animation:borderSpin 10s linear infinite;
}
.dropdown-search{

width: 100%;
height:64px;

padding:0 24px 0 28px;

border:none;
outline:none;
border-radius:34px;
font-family:
        "Manrope",
        "Plus Jakarta Sans",
        "Avenir Next",
        "SF Pro Display",
        sans-serif;

font-size:20px;
font-weight:150;
letter-spacing:-0.01em;

background:
radial-gradient(
circle at 15% 20%,
rgba(255,223,140,.18),
transparent 35%
),
radial-gradient(
circle at 85% 80%,
rgba(255,210,110,.12),
transparent 40%
),
linear-gradient(
135deg,
rgba(255,255,255,.82),
rgba(255,255,255,.55)
);

backdrop-filter:blur(20px);

color:#222;

box-shadow:
inset 0 1px 0 rgba(255,255,255,.95),
inset 0 0 35px rgba(255,223,140,.18),
inset 0 0 80px rgba(255,240,200,.18),
0 10px 30px rgba(0,0,0,.06);

transition:.35s ease;

-webkit-appearance:none;
appearance:none;

}

.dropdown-search:focus{

box-shadow:
inset 0 1px 0 rgba(255,255,255,1),
inset 0 0 45px rgba(255,220,120,.28),
inset 0 0 100px rgba(255,240,190,.22),
0 0 30px rgba(255,215,120,.18),
0 15px 40px rgba(255,205,110,.22);

}

.dropdown-search::placeholder{

    color:#a3a3a3;

    font-family:
        "Manrope",
        "Plus Jakarta Sans",
        "Avenir Next",
        "SF Pro Display",
        sans-serif;

    font-size:18px;
    font-weight:300;
    letter-spacing:-0.03em;
    line-height:64px;

}


.clear-btn{
    position:absolute;
    right:20px;
    
    top:50%;
    transform:translateY(-50%);

    z-index:2;

    width:42px;
    height:42px;

    display:none;
    align-items:center;
    justify-content:center;

    padding:0;
    margin:0;

    border-radius:30%;
    cursor:pointer;

    background:
        linear-gradient(
            145deg,
            #ffffff,
            #f7f3ea
        );
    
    color:#b88624;
    font-size:30px;
    font-weight:100;
    line-height:-4;

    backdrop-filter:blur(10px);

    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.95),
        0 5px 15px rgba(0,0,0,.08);
        
    transition:
        transform .3s ease,
        box-shadow .3s ease;
}

.clear-btn span{
    margin:14px;
    padding:0;
    
    transform:translateY(-1px);
    
    user-select:none;
    pointer-events:none;
    

}

.clear-btn:hover{
    transform:
        translateY(-50%)
        scale(1.08);

    box-shadow:
        0 8px 22px rgba(255,205,110,.28);
}
.dropdown-list{
    display:none;
    margin-top:16px;
    max-height:360px;
    overflow-y:auto;

    border-radius:28px;

    background:rgba(255,255,255,.9);
    backdrop-filter:blur(24px);

    border:0px solid rgba(255,255,255,.8);

    box-shadow:
        0 18px 45px rgba(0,0,0,.08),
        inset 0 1px 0 rgba(255,255,255,.9);
}

.dropdown.open .dropdown-list{
    display:block;
}

.dropdown-item{
    padding:18px 24px;
    font-size:14px;
    color:#2a2a2a;
    cursor:pointer;

    transition:.25s ease;

    border-bottom:1px solid rgba(0,0,0,.05);
}

.dropdown-item:last-child{
    border-bottom:none;
}

.dropdown-item:hover{
    padding-left:34px;
    background:rgba(255,220,140,.12);
    color:#111;
}

.dropdown-list::-webkit-scrollbar{
    width:5px;
}

.dropdown-list::-webkit-scrollbar-track{
    background:transparent;
}

.dropdown-list::-webkit-scrollbar-thumb{
    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,.55) 0%,
            rgba(255,248,225,.45) 35%,
            rgba(255,220,140,.30) 70%,
            rgba(255,255,255,.50) 100%
        );

    border:1px solid rgba(255,255,255,.25);

    backdrop-filter:blur(12px);
    -webkit-backdrop-filter:blur(12px);

    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.55),
        0 0 8px rgba(255,220,140,.12),
        0 1px 4px rgba(0,0,0,.04);

    transition:
        background .25s ease,
        box-shadow .25s ease;
}

.dropdown-list::-webkit-scrollbar-thumb:hover{
    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,.70) 0%,
            rgba(255,245,210,.60) 40%,
            rgba(255,220,140,.45) 70%,
            rgba(255,255,255,.65) 100%
        );

    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.70),
        0 0 12px rgba(255,220,140,.18),
        0 2px 6px rgba(0,0,0,.05);
}




/* ================= SUB FILTER ================= */
#subDropdown {
    margin-top: 16px;

    background: rgba(255,255,255,0.88);
    border-radius: 20px;

    padding: 18px;

    backdrop-filter: blur(14px);

    border: 4px solid rgba(0,0,0,0.04);

    box-shadow:
        0 20px 50px rgba(0,0,0,0.07),
        0 1px 0 rgba(255,255,255,0.7) inset;
}

/* ================= FILTER GROUP ================= */
.filter-group {
    margin-bottom: 18px;
}

.filter-title {
    font-weight: 600;
    margin-bottom: 10px;

    font-size: 12px;
    letter-spacing: 0.6px;
    text-transform: uppercase;

    color: #6b7280;
}

/* ================= OPTIONS ================= */
.filter-options {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

/* pills */
.sub-item {
    padding: 9px 14px;

    background: #f1f5f9;

    border-radius: 999px;

    cursor: pointer;

    font-size: 13px;
    font-weight: 500;

    color: #334155;

    border: 1px solid rgba(0,0,0,0.05);

    transition: all 0.18s ease;
}

.sub-item:hover {
    background: #eaf6ff;

    transform: translateY(-1px);

    box-shadow:
        0 6px 12px rgba(0,0,0,0.06);
}

.sub-item.selected {
    background: linear-gradient(90deg, #0072ff, #00c6ff);
    color: #fff;

    border-color: transparent;

    box-shadow:
        0 10px 22px rgba(0,114,255,0.25);
}

/* ================= SUB SEARCH ================= */
.sub-search {
    width: 40%;
    padding: 10px 14px;

    margin: 12px 0;

    border-radius: 12px;

    border: 1px solid rgba(0,0,0,0.06);

    font-size: 13px;

    background: rgba(255,255,255,0.9);

    transition: all 0.18s ease;
}

.sub-search:focus {
    border-color: rgba(0,114,255,0.35);

    box-shadow:
        0 0 0 3px rgba(0,114,255,0.10);
}

/* ================= SHOW MORE ================= */
.show-more {
    margin-top: 12px;

    font-size: 12px;
    font-weight: 500;

    color: #6b7280;

    cursor: pointer;

    display: inline-flex;
    align-items: center;
    gap: 4px;

    transition: all 0.18s ease;
}

.show-more:hover {
    color: #111827;
    transform: translateX(3px);
}

/* ================= ANIMATION ================= */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(6px) scale(0.98);
    }
    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}
.price-range {
    position: relative;
    width: 100%;
    padding: 50px 8px 10px;
    box-sizing: border-box;
    top: 100px;
    display: flex;
    flex-direction: column;
    gap: 22px;
    background: transparent;
}

.price-field {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 100px;
}

.price-field label {
    font-size: 12px;
    font-weight: 600;
    color: #6b7280;
    padding-left: 2px;
}

.price-range::before {
    content: "";
    position: absolute;
    left: 8px;
    right: 8px;
    top: 22px;
    height: 2px;
    border-radius: 999px;
    z-index: 1;

    background: linear-gradient(180deg, #effcff 0%, #8eeaff 100%);

    box-shadow:
        inset 0 1px 2px rgba(0,0,0,0.10),
        inset 0 -1px 2px rgba(255,255,255,0.7);
}

.price-range::after {
    content: "";
    position: absolute;
    left: calc(8px + var(--min) - 5px);
    right: calc(100% - var(--max));
    top: 22px;
    height: 2px;
    border-radius: 999px;
    z-index: 2;

    background: linear-gradient(90deg, #8eeaff 0%, #54dbff 50%, #38cfff 100%);

    box-shadow:
        inset 0 1px 1px rgba(255,255,255,0.35),
        inset 0 -1px 2px rgba(0,0,0,0.12),
        0 2px 6px rgba(120,240,255,0.35);
}
/* ================= FLOATING DOT WAVES ================= */

.bar{
    position: absolute;

    /* התחלה */
    left: calc(4px + var(--min) - 1px);
    top: -21px;

    /* סוף */
    right: calc(100% - var(--max));

    height: 150px;

    overflow: hidden;




}
.bar::before{
    content: "";

    position: absolute;

    inset: 0;

    border-radius: inherit;

    pointer-events: none;

    box-shadow:
        inset 0 0 22px rgba(255,255,255,.15),
        0 0 24px rgba(0,180,255,.12);

    z-index: 4;
}

/* =========================================
   INNER
========================================= */
.bar-inner{
    position: absolute;
    inset: 0;
    top:-5px ;
    overflow: hidden;
    pointer-events: none;

    z-index: 2;

    border-radius: inherit;

    height: 50px;
}

/* =========================================
   WAVE
========================================= */
.wave{
    position:absolute;

    left:0;
    top:20px;

    width:100%;
    height:36px;

    transform:translateY(-50%);
    transform-origin:center center;

    overflow:hidden;

    pointer-events:none;
    isolation:isolate;

    opacity:.98;

    will-change:
        transform,
        filter,
        clip-path;

    filter:
        blur(.12px)
        saturate(1.35)
        brightness(1.04);

    clip-path:
        polygon(
            0% 58%,
            4% 52%,
            8% 64%,
            12% 48%,
            16% 61%,
            20% 45%,
            24% 66%,
            28% 54%,
            32% 62%,
            36% 46%,
            40% 57%,
            44% 51%,
            48% 68%,
            52% 48%,
            56% 60%,
            60% 45%,
            64% 67%,
            68% 54%,
            72% 62%,
            76% 47%,
            80% 58%,
            84% 52%,
            88% 66%,
            92% 54%,
            96% 61%,
            100% 50%,
            100% 100%,
            0% 100%
        );

    animation:
        googleSurface 5s ease-in-out infinite;
}

/* =========================================
   MAIN GOOGLE LIQUID
========================================= */
.wave::before{
    content:"";

    position:absolute;
    inset:-25%;

    background:
        linear-gradient(
            90deg,

            #4285F4 0%,
            #6EA8FF 8%,
            #8FC2FF 14%,

            #34A853 24%,
            #7BDC6B 33%,

            #FBBC05 46%,
            #FFE27A 56%,

            #EA4335 68%,
            #FF9B91 78%,

            #4285F4 90%,
            #8FC2FF 100%
        );

    background-size:400% 100%;

    filter:
        blur(.2px)
        saturate(1.3);

    box-shadow:
        0 0 10px rgba(66,133,244,.20),
        0 0 20px rgba(66,133,244,.18),
        0 0 35px rgba(52,168,83,.16),
        0 0 55px rgba(251,188,5,.14),
        0 0 85px rgba(234,67,53,.10),

        inset 0 2px 5px rgba(255,255,255,.35),
        inset 0 -4px 10px rgba(66,133,244,.12);

    animation:
        googleFlow 4s linear infinite,
        googleBreath 3.5s ease-in-out infinite;
}

/* =========================================
   PREMIUM GLASS SHINE
========================================= */
.wave::after{
    content:"";

    position:absolute;
    inset:0;

    background:

        radial-gradient(
            circle at 8% 35%,
            rgba(255,255,255,.95),
            transparent 10%
        ),

        radial-gradient(
            circle at 28% 45%,
            rgba(255,255,255,.55),
            transparent 18%
        ),

        radial-gradient(
            circle at 52% 40%,
            rgba(255,255,255,.45),
            transparent 22%
        ),

        radial-gradient(
            circle at 75% 42%,
            rgba(255,255,255,.55),
            transparent 18%
        ),

        radial-gradient(
            circle at 92% 35%,
            rgba(255,255,255,.95),
            transparent 10%
        ),

        linear-gradient(
            180deg,
            rgba(255,255,255,.42),
            transparent 35%
        ),

        linear-gradient(
            90deg,
            rgba(255,255,255,.95),
            rgba(255,255,255,.08),
            rgba(255,255,255,.95)
        );

    mix-blend-mode:screen;

    animation:
        googleShimmer 2.5s linear infinite,
        googleGlow 3s ease-in-out infinite;
}

/* =========================================
   COLOR FLOW
========================================= */
@keyframes googleFlow{

    0%{
        background-position:0% 0%;
    }

    100%{
        background-position:400% 0%;
    }
}

/* =========================================
   LIQUID SURFACE
========================================= */
@keyframes googleSurface{

    0%{
        clip-path:
            polygon(
                0% 56%,
                8% 64%,
                16% 53%,
                24% 67%,
                32% 47%,
                40% 58%,
                48% 66%,
                56% 53%,
                64% 64%,
                72% 49%,
                80% 59%,
                88% 65%,
                100% 54%,
                100% 100%,
                0% 100%
            );
    }

    25%{
        clip-path:
            polygon(
                0% 51%,
                8% 61%,
                16% 46%,
                24% 64%,
                32% 52%,
                40% 68%,
                48% 54%,
                56% 63%,
                64% 47%,
                72% 58%,
                80% 50%,
                88% 67%,
                100% 57%,
                100% 100%,
                0% 100%
            );
    }

    50%{
        clip-path:
            polygon(
                0% 63%,
                8% 49%,
                16% 66%,
                24% 52%,
                32% 61%,
                40% 45%,
                48% 68%,
                56% 51%,
                64% 63%,
                72% 47%,
                80% 65%,
                88% 53%,
                100% 60%,
                100% 100%,
                0% 100%
            );
    }

    75%{
        clip-path:
            polygon(
                0% 54%,
                8% 66%,
                16% 50%,
                24% 61%,
                32% 44%,
                40% 65%,
                48% 53%,
                56% 64%,
                64% 48%,
                72% 57%,
                80% 50%,
                88% 66%,
                100% 55%,
                100% 100%,
                0% 100%
            );
    }

    100%{
        clip-path:
            polygon(
                0% 56%,
                8% 64%,
                16% 53%,
                24% 67%,
                32% 47%,
                40% 58%,
                48% 66%,
                56% 53%,
                64% 64%,
                72% 49%,
                80% 59%,
                88% 65%,
                100% 54%,
                100% 100%,
                0% 100%
            );
    }
}

/* =========================================
   SOFT GOOGLE BREATH
========================================= */
@keyframes googleBreath{

    0%{
        transform:
            scaleY(.93)
            scaleX(1);
    }

    50%{
        transform:
            scaleY(1.10)
            scaleX(1.015);
    }

    100%{
        transform:
            scaleY(.93)
            scaleX(1);
    }
}

/* =========================================
   LIGHT SWEEP
========================================= */
@keyframes googleShimmer{

    0%{
        transform:
            translateX(-100px);
    }

    100%{
        transform:
            translateX(100px);
    }
}

/* =========================================
   PREMIUM GLOW
========================================= */
@keyframes googleGlow{

    0%{
        opacity:.65;
        filter:blur(0px);
    }

    50%{
        opacity:1;
        filter:blur(.25px);
    }

    100%{
        opacity:.65;
        filter:blur(0px);
    }
}
.min-slider,
.max-slider{
    width:100%;
    position:absolute;
    left:0;
    top:-18px;
    transform:translateY(-40%);
    pointer-events:none;
    -webkit-appearance:none;
    appearance:none;
    background:transparent;
    z-index:30;
}

/* ==================================================
   MINIMAL GOOGLE / APPLE STYLE
   ================================================== */

.min-slider::-webkit-slider-thumb,
.max-slider::-webkit-slider-thumb{

    pointer-events:all;

    -webkit-appearance:none;
    appearance:none;

    width:8px;
    height:88px;

    cursor:pointer;
    border:none;

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #f5f7fa 18%,
            #e8edf3 45%,
            #dce4ec 100%
        );

    box-shadow:
        0 2px 6px rgba(0,0,0,.08),
        0 8px 18px rgba(0,0,0,.10),
        inset 0 1px 0 rgba(255,255,255,.95);

    transition:
        transform .18s ease,
        box-shadow .18s ease;
}

/* subtle blue center line */

.min-slider::-webkit-slider-thumb::before,
.max-slider::-webkit-slider-thumb::before{

    content:"";

    position:absolute;

    left:50%;
    top:50%;

    width:2px;
    height:34px;

    transform:translate(-50%,-50%);

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #8ab4f8,
            #4285f4
        );

    opacity:.9;
}

/* hover */

.min-slider::-webkit-slider-thumb:hover,
.max-slider::-webkit-slider-thumb:hover{

    transform:scaleX(1.08);

    box-shadow:
        0 3px 8px rgba(0,0,0,.10),
        0 10px 22px rgba(0,0,0,.12),
        inset 0 1px 0 rgba(255,255,255,1);
}

/* drag */

.min-slider::-webkit-slider-thumb:active,
.max-slider::-webkit-slider-thumb:active{

    transform:scaleX(1.15);

    box-shadow:
        0 2px 5px rgba(0,0,0,.10),
        0 6px 14px rgba(0,0,0,.12),
        inset 0 1px 0 rgba(255,255,255,1);
}

/* Firefox */

.min-slider::-moz-range-thumb,
.max-slider::-moz-range-thumb{

    width:8px;
    height:88px;

    border:none;
    cursor:pointer;

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #f5f7fa 18%,
            #e8edf3 45%,
            #dce4ec 100%
        );

    box-shadow:
        0 2px 6px rgba(0,0,0,.08),
        0 8px 18px rgba(0,0,0,.10);
}

/* =========================================
   PRICE VALUES
========================================= */
.price-values{
    display:flex;
    justify-content:space-between;
    align-items:flex-start;
    gap:12px;
    margin-top: 80px;
    margin-bottom: 200px;

}

.price-values .value-box{
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:6px;
    flex:1;
    margin-top:40px;
}

.price-values .value-box span{
    font-size:10px;
    font-weight:700;
    letter-spacing:1px;
    text-transform:uppercase;
    color:#7a8ca5;
}

/* =========================================================
   PREMIUM INPUT FIELD (PRICE VALUES)
   Clean Apple-like UI • Soft Depth • Subtle Glow • Smooth Feel
========================================================= */

.price-values input{

    width:100%;
    max-width:90px;
    height:75px;

    padding:0 12px;

    border:none;
    outline:none;

    border-radius:16px;

    background:
        linear-gradient(145deg, #ffffff, #f6fbff);

    text-align:center;

    font-size:18px;
    font-weight:500;
    letter-spacing:0.3px;

    color:#00a8e8;

    box-shadow:
        inset 0 1px 0 rgba(255,255,255,0.9),
        0 10px 20px rgba(0,0,0,0.05),
        0 0 0 1px rgba(0,181,241,0.10);

    transition:
        transform 0.22s cubic-bezier(.2,.9,.2,1),
        box-shadow 0.22s ease,
        background 0.22s ease,
        color 0.22s ease;
}

/* hover floating */
.price-values input:hover{

    transform:
        translateY(-2px) scale(1.03);

    box-shadow:
        0 14px 28px rgba(0,0,0,0.08),
        0 0 0 1px rgba(0,181,241,0.18);
}

/* focus glow */
.price-values input:focus{

    color:#007fd1;

    background:
        linear-gradient(145deg, #ffffff, #eef9ff);

    box-shadow:
        0 0 0 4px rgba(0,181,241,0.12),
        0 18px 40px rgba(0,181,241,0.15);
}

/* =========================================
   CURRENCY
========================================= */
/* =========================
   CLEAN PREMIUM CURRENCY BOX
========================= */

*{
    box-sizing:border-box;
}

@keyframes fadeIn{
    from{
        opacity:0;
        transform:translateY(10px);
    }
    to{
        opacity:1;
        transform:none;
    }
}

@keyframes glowMove{
    0%,100%{
        background-position:0% 50%;
    }
    50%{
        background-position:100% 50%;
    }
}

.currency-box{
    position:relative;
    display:flex;
    flex-direction:column;

    top:-320px;
    margin:-160px auto 10px;

font-family:
    "Satoshi",
    "Inter",
    sans-serif;

    font-size:18px;
    font-weight:300;
}

/* =========================
   HEADER
========================= */

.currency-header{
    position:relative;
    overflow:hidden;

    padding:18px 20px;

    border-radius:24px;
    cursor:pointer;

    background:
        radial-gradient(
            circle at 20% 30%,
            rgba(255,215,120,.18),
            transparent 35%
        ),
        radial-gradient(
            circle at 80% 70%,
            rgba(255,230,170,.15),
            transparent 40%
        ),
        linear-gradient(
            180deg,
            rgba(255,255,255,.95),
            rgba(248,248,248,.92)
        );

    backdrop-filter:blur(18px);

    border:1px solid rgba(255,230,180,.45);

    box-shadow:
        0 12px 35px rgba(0,0,0,.07),
        0 0 24px rgba(255,210,120,.08),
        inset 0 1px 0 rgba(255,255,255,.9);

    transition:.3s ease;
}

.currency-header:hover{
    transform:translateY(-3px);

    box-shadow:
        0 18px 45px rgba(255,210,120,.14),
        0 10px 25px rgba(0,0,0,.07);
}

.currency-box.open .currency-header{
    box-shadow:
        0 20px 55px rgba(255,210,120,.18),
        0 0 0 1px rgba(255,220,150,.25);
}

/* =========================
   DROPDOWN
========================= */

.currency-dropdown{
    display:none;

    position:absolute;
    top:calc(100% + 14px);

    width:180px;
    z-index:50;

    padding:16px;

    border-radius:24px;

    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,.96),
            rgba(248,248,248,.93)
        );

    backdrop-filter:blur(20px);

    border:1px solid rgba(255,235,190,.45);

    box-shadow:
        0 25px 60px rgba(0,0,0,.10),
        0 0 30px rgba(255,215,140,.10);

    animation:fadeIn .25s ease;
}

.currency-box.open .currency-dropdown{
    display:block;
}

/* =========================
   SEARCH
========================= */

.currency-search{
    width:100%;

    padding:13px 15px;

    margin-bottom:14px;

    border:none;
    outline:none;

    border-radius:16px;

    background:rgba(255,255,255,.9);

    box-shadow:
        inset 0 0 0 1px rgba(0,0,0,.05);

    transition:.25s ease;
}

.currency-search::placeholder{
    color:#a0a0a0;
}

.currency-search:focus{
    transform:translateY(-2px);

    box-shadow:
        0 0 0 4px rgba(255,215,120,.14),
        0 10px 25px rgba(255,215,120,.12);
}

/* =========================
   LIST
========================= */

.currency-list{
    max-height:260px;
    overflow-y:auto;

    scrollbar-width:thin;
    scrollbar-color:rgba(255,215,140,.7) transparent;
}

.currency-list::-webkit-scrollbar{
    width:4px;
}

.currency-list::-webkit-scrollbar-thumb{
    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #fff8df 10%,
            #ffeebd 25%,
            #ffd977 50%,
            #f0c25b 75%,
            #fff1c8 100%
        );

    box-shadow:
        inset 0 1px 2px rgba(255,255,255,1),
        inset 0 -1px 1px rgba(180,130,40,.18),
        0 0 10px rgba(255,220,140,.45),
        0 2px 8px rgba(0,0,0,.10);
}

.currency-list::-webkit-scrollbar-thumb:hover{
    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #fffdf4 10%,
            #fff2c9 25%,
            #ffe18a 50%,
            #f8ca63 75%,
            #fff6da 100%
        );

    box-shadow:
        inset 0 1px 2px rgba(255,255,255,1),
        inset 0 -1px 1px rgba(180,130,40,.12),
        0 0 18px rgba(255,225,150,.65),
        0 4px 14px rgba(0,0,0,.12);
}

/* =========================
   ITEM
========================= */

.currency-item{
    padding:14px;
    border-radius:16px;

    cursor:pointer;

    transition:.25s ease;
}

.currency-item:hover{
    background:
        linear-gradient(
            135deg,
            rgba(255,220,130,.14),
            rgba(255,255,255,.6)
        );

    transform:translateX(4px);
}

.currency-item.active{
    color:#6f4a00;

    background:
        linear-gradient(
            135deg,
            #fff8e6,
            #ffe8b0,
            #ffd98a
        );

    background-size:200% 200%;
    animation:glowMove 6s linear infinite;

    box-shadow:
        0 10px 24px rgba(255,190,80,.18),
        0 0 18px rgba(255,215,140,.15);

    font-weight:500;
}

/* =========================================
   CATEGORIES
========================================= */
.categories{

    display:flex;
    flex-wrap:wrap;

    justify-content:center;

    gap:14px;

    padding:16px;
}

.circle{

    width:74px;
    height:74px;

    overflow:hidden;

    border-radius:50%;

    background:#fff;

    box-shadow:
        0 4px 14px rgba(0,0,0,.05);

    transition:
        .18s ease;
}

.category:hover .circle{

    transform:
        scale(1.06);

    box-shadow:
        0 8px 20px rgba(0,0,0,.08);
}

.category.active .circle{

    transform:
        scale(1.06);

    box-shadow:
        0 0 0 3px rgba(0,181,241,.18),
        0 8px 20px rgba(0,181,241,.15);
}

.category.hidden{

    opacity:0;

    transform:
        scale(.85);

    pointer-events:none;

    position:absolute;
}

/* =========================================
   FOOTER
========================================= */
.bottom-footer{

    margin-top:50px;

    padding:16px;

    display:flex;
    justify-content:center;

    gap:20px;

    font-size:12px;
}

.bottom-footer a{

    color:#7a8ca5;

    text-decoration:none;

    transition:.15s ease;
}

.bottom-footer a:hover{
    color:#00b5f1;
}

/* =========================================
   FLOAT BUTTON
========================================= */
#floatingSearchBtn{

    position:fixed;

    bottom:20px;
    right:20px;

    padding:14px 24px;

    border:none;

    border-radius:16px;

    background:
        linear-gradient(
            135deg,
            #00c2ff,
            #00a8f5
        );

    color:white;

    font-size:14px;
    font-weight:700;

    cursor:pointer;

    box-shadow:
        0 10px 30px rgba(0,181,241,.22);

    opacity:0;
    visibility:hidden;

    transform:
        translateY(15px);

    transition:
        .22s ease;
}

#floatingSearchBtn.show{

    opacity:1;
    visibility:visible;

    transform:
        translateY(0);
}

#floatingSearchBtn:hover{

    transform:
        translateY(-2px);

    box-shadow:
        0 14px 35px rgba(0,181,241,.28);
}
/* =========================================
   ANIMATIONS
========================================= */
@keyframes paintBlob{

    0%{
        border-radius:
            52% 48% 58% 42% /
            42% 57% 43% 58%;
    }

    25%{
        border-radius:
            63% 37% 46% 54% /
            54% 42% 58% 46%;
    }

    50%{
        border-radius:
            40% 60% 52% 48% /
            61% 38% 62% 39%;
    }

    75%{
        border-radius:
            57% 43% 63% 37% /
            44% 58% 42% 56%;
    }

    100%{
        border-radius:
            52% 48% 58% 42% /
            42% 57% 43% 58%;
    }
}

@keyframes brushTextFloat{

    0%,100%{
        transform:translateY(0);
    }

    50%{
        transform:translateY(-2px);
    }
}

@keyframes dropPaint{

    from{
        opacity:0;
        transform:
            translateY(-12px)
            scale(.95);
    }

    to{
        opacity:1;
        transform:
            translateY(0)
            scale(1);
    }
}



////////////////////////////////////
//////////////////////////////////////
////////////////_////////////////////////

/* ================= BODY ================= */
body {
    margin: 0;
    font-family: 'Inter', Arial, sans-serif;
    color: #1f2937;

    background: linear-gradient(
        180deg,
        #f7f9fc 0%,
        #eef2f7 55%,
        #e6edf6 100%
    );

    background-attachment: fixed;
}

/* סיסטם חלק */
* {
    -webkit-tap-highlight-color: transparent;
    box-sizing: border-box;
}

/* ================= NAVBAR ================= */
.top-navbar {
    position: sticky;
    top: 0;
    z-index: 1000;

    display: flex;
    justify-content: space-between;
    align-items: center;

    padding: 14px 24px;

    background: rgba(255,255,255,0.55);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);

    border-bottom: 1px solid rgba(0,0,0,0.05);

    box-shadow:
        0 10px 35px rgba(0,0,0,0.07),
        0 1px 0 rgba(255,255,255,0.7) inset;

    transition: transform 0.25s ease, opacity 0.25s ease;
}

.top-navbar.scrolled {
    transform: translateY(-110%);
    opacity: 0;
    pointer-events: none;
}

.top-navbar::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
        90deg,
        rgba(0,114,255,0.06),
        rgba(0,198,255,0.04),
        transparent
    );
    pointer-events: none;
}

.top-navbar .logo {
    font-weight: 800;
    font-size: 15px;

    background: linear-gradient(90deg, #0072ff, #00c6ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;

    letter-spacing: 0.5px;
}

/* ================= NAV LINKS ================= */
.nav-links {
    display: flex;
    gap: 20px;
    align-items: center;
}

.nav-links a {
    font-size: 13px;
    color: #6b7280;
    text-decoration: none;

    position: relative;
    padding: 6px 4px;

    transition: all 0.25s ease;
}

.nav-links a::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -4px;

    width: 0%;
    height: 2px;
    border-radius: 999px;

    background: linear-gradient(90deg, #0072ff, #00c6ff);

    transition: width 0.3s ease;
}

.nav-links a:hover {
    color: #0072ff;
    transform: translateY(-1px);
}

.nav-links a:hover::after {
    width: 100%;
}

/* אפקט עדין של עומק בלינקים */
.nav-links a:active {
    transform: scale(0.96);
}

/* ================= TITLE ================= */
.search-label{
    position:;

    display:flex;
    justify-content:center;
    align-items:center;

    margin-top:55px;
    margin-bottom:35px;

    font-size:clamp(28px,4vw,48px);
    font-weight:200;

    letter-spacing:-1.2px;
    line-height:1.05;

    text-align:center;

    background:
        linear-gradient(
            90deg,
            #4285F4 0%,
            #6FAEFF 12%,
            #34A853 28%,
            #86E56F 42%,
            #FBBC05 58%,
            #FFE27A 70%,
            #EA4335 86%,
            #FF8F84 100%
        );

    background-size:300% 100%;

    -webkit-background-clip:text;
    background-clip:text;
    -webkit-text-fill-color:transparent;

    filter:
        drop-shadow(0 0 12px rgba(66,133,244,.18))
        drop-shadow(0 0 20px rgba(52,168,83,.10));

    animation:
        googleTextFlow 10s linear infinite,
        googleFloat 6s ease-in-out infinite;
}

/* glass aura */
.search-label::before{
    content:"";

    position:absolute;

    width:80%;
    height:220%;

    left:50%;
    top:50%;

    transform:
        translate(-50%,-50%);

    border-radius:999px;

    background:
        radial-gradient(
            circle,
            rgba(66,133,244,.12),
            rgba(52,168,83,.08),
            rgba(251,188,5,.06),
            transparent 70%
        );

    filter:blur(45px);

    z-index:-2;

    animation:
        googleAura 8s ease-in-out infinite;
}

/* premium animated underline */
.search-label::after{
    content:"";

    position:absolute;

    left:50%;
    bottom:-16px;

    transform:
        translateX(-50%);

    width:180px;
    height:4px;

    border-radius:999px;

    background:
        linear-gradient(
            90deg,
            #4285F4,
            #34A853,
            #FBBC05,
            #EA4335,
            #4285F4
        );

    background-size:300% 100%;

    box-shadow:
        0 0 10px rgba(66,133,244,.25),
        0 0 20px rgba(52,168,83,.18),
        0 0 30px rgba(234,67,53,.12);

    animation:
        googleLineFlow 4s linear infinite;
}

/* top sparkle */
.search-label span{
    position:relative;
}

.search-label span::before{
    content:"✦";

    position:absolute;

    top:-18px;
    right:-22px;

    font-size:14px;

    background:
        linear-gradient(
            135deg,
            #4285F4,
            #34A853,
            #FBBC05,
            #EA4335
        );

    -webkit-background-clip:text;
    -webkit-text-fill-color:transparent;

    animation:
        sparklePulse 2.5s ease-in-out infinite;
}

@keyframes googleTextFlow{

    0%{
        background-position:0% 50%;
    }

    100%{
        background-position:300% 50%;
    }
}

@keyframes googleLineFlow{

    0%{
        background-position:0% 50%;
    }

    100%{
        background-position:300% 50%;
    }
}

@keyframes googleFloat{

    0%{
        transform:translateY(0px);
    }

    50%{
        transform:translateY(-2px);
    }

    100%{
        transform:translateY(0px);
    }
}

@keyframes googleAura{

    0%{
        opacity:.65;
        transform:
            translate(-50%,-50%)
            scale(1);
    }

    50%{
        opacity:1;
        transform:
            translate(-50%,-50%)
            scale(1.08);
    }

    100%{
        opacity:.65;
        transform:
            translate(-50%,-50%)
            scale(1);
    }
}

@keyframes sparklePulse{

    0%{
        opacity:.4;
        transform:
            scale(.8)
            rotate(0deg);
    }

    50%{
        opacity:1;
        transform:
            scale(1.25)
            rotate(25deg);
    }

    100%{
        opacity:.4;
        transform:
            scale(.8)
            rotate(0deg);
    }
}

/* ================= SEARCH ================= */
/* =========================================================
   APPLE / OPENAI LEVEL PREMIUM TEXTAREA
========================================================= */

@property --angle{
    syntax:"<angle>";
    inherits:false;
    initial-value:0deg;
}

@property --x{
    syntax:"<percentage>";
    inherits:false;
    initial-value:20%;
}

@property --y{
    syntax:"<percentage>";
    inherits:false;
    initial-value:30%;
}

@property --x2{
    syntax:"<percentage>";
    inherits:false;
    initial-value:80%;
}

@property --y2{
    syntax:"<percentage>";
    inherits:false;
    initial-value:70%;
}

/* =========================================================
   CONTAINER
========================================================= */

.describe-box{
    display:flex;
    justify-content:center;
    margin:42px 15px;
    position:relative;
}

/* =========================================================
   TEXTAREA
========================================================= */

.describe-box textarea{
    width:92vw;
    max-width:760px;
    min-height:130px;

    padding:22px 24px;

    border:2px solid transparent;
    border-radius:30px;

    outline:none;
    resize:none;

    color:#111827;
    font-size:15px;
    line-height:1.7;
    font-weight:200;

    position:relative;

    background:

        radial-gradient(
            circle at var(--x) var(--y),
            rgba(66,133,244,.18),
            transparent 22%
        ),

        radial-gradient(
            circle at var(--x2) var(--y2),
            rgba(52,168,83,.14),
            transparent 24%
        ),

        radial-gradient(
            circle at 80% 18%,
            rgba(251,188,5,.12),
            transparent 18%
        ),

        radial-gradient(
            circle at 20% 85%,
            rgba(234,67,53,.10),
            transparent 20%
        ),

        linear-gradient(
            rgba(255,255,255,.90),
            rgba(255,255,255,.88)
        ) padding-box,

        conic-gradient(
            from var(--angle),
            #4285f4,
            #64a3ff,
            #34a853,
            #72db78,
            #fbbc05,
            #ffe08a,
            #ea4335,
            #ff8d86,
            #4285f4
        ) border-box;

    backdrop-filter:blur(24px) saturate(170%);
    -webkit-backdrop-filter:blur(24px) saturate(170%);

    box-shadow:
        0 30px 80px rgba(15,23,42,.10),
        0 15px 35px rgba(15,23,42,.06),
        inset 0 1px 0 rgba(255,255,255,.95),
        inset 0 -1px 0 rgba(255,255,255,.40),
        0 0 0 1px rgba(255,255,255,.65);

    animation:
        borderSpin 10s linear infinite,
        liquidMove 18s ease-in-out infinite,
        breathingGlow 6s ease-in-out infinite;

    transition:
        transform .45s cubic-bezier(.22,1,.36,1),
        box-shadow .45s cubic-bezier(.22,1,.36,1),
        background .45s ease;
}

/* =========================================================
   PLACEHOLDER
========================================================= */

.describe-box textarea::placeholder{
    color:rgba(55,65,81,.55);
    font-family:
    "Satoshi",
    "Manrope",
    "Inter",
    "SF Pro Display",
    "Segoe UI",
    Roboto,
    Arial,
    sans-serif;
    font-size: 17px;
    font-weight:300;
    letter-spacing:.2px;
}

/* =========================================================
   HOVER
========================================================= */

.describe-box textarea:hover{

    transform:
        translateY(-3px);

    box-shadow:
        0 40px 90px rgba(66,133,244,.12),
        0 18px 40px rgba(15,23,42,.08),
        inset 0 1px 0 rgba(255,255,255,.95),
        0 0 40px rgba(66,133,244,.08);
}

/* =========================================================
   FOCUS
========================================================= */

.describe-box textarea:focus{

    transform:
        translateY(-5px)
        scale(1.01);

    background:

        radial-gradient(
            circle at var(--x) var(--y),
            rgba(66,133,244,.24),
            transparent 25%
        ),

        radial-gradient(
            circle at var(--x2) var(--y2),
            rgba(52,168,83,.18),
            transparent 26%
        ),

        radial-gradient(
            circle at 80% 18%,
            rgba(251,188,5,.18),
            transparent 18%
        ),

        radial-gradient(
            circle at 20% 85%,
            rgba(234,67,53,.15),
            transparent 20%
        ),

        linear-gradient(
            rgba(255,255,255,.96),
            rgba(255,255,255,.92)
        ) padding-box,

        conic-gradient(
            from var(--angle),
            #4285f4,
            #6fb3ff,
            #34a853,
            #8df18d,
            #fbbc05,
            #ffe27c,
            #ea4335,
            #ff9f95,
            #4285f4
        ) border-box;

    box-shadow:
        0 50px 120px rgba(66,133,244,.18),
        0 25px 55px rgba(15,23,42,.12),
        0 0 0 5px rgba(66,133,244,.08),
        0 0 40px rgba(66,133,244,.15),
        0 0 60px rgba(52,168,83,.10),
        inset 0 1px 0 rgba(255,255,255,.95);

    animation:
        borderSpin 4s linear infinite,
        liquidMove 10s ease-in-out infinite,
        breathingGlow 2.5s ease-in-out infinite;
}

/* =========================================================
   SCROLLBAR
========================================================= */

/* =========================================================
   SOFT MINIMAL SCROLLBAR (TEXTAREA)
========================================================= */


/* =========================================================
   ANIMATIONS
========================================================= */

@keyframes borderSpin{

    from{
        --angle:0deg;
    }

    to{
        --angle:360deg;
    }
}

@keyframes breathingGlow{

    0%{
        filter:
            brightness(1)
            saturate(1);
    }

    50%{
        filter:
            brightness(1.08)
            saturate(1.15);
    }

    100%{
        filter:
            brightness(1)
            saturate(1);
    }
}

@keyframes liquidMove{

    0%{
        --x:18%;
        --y:25%;

        --x2:82%;
        --y2:70%;
    }

    25%{
        --x:75%;
        --y:18%;

        --x2:28%;
        --y2:80%;
    }

    50%{
        --x:88%;
        --y:70%;

        --x2:18%;
        --y2:30%;
    }

    75%{
        --x:35%;
        --y:88%;

        --x2:82%;
        --y2:22%;
    }

    100%{
        --x:18%;
        --y:25%;

        --x2:82%;
        --y2:70%;
    }
}

/* =========================================================
   ANIMATIONS
========================================================= */

/* =========================================================
   APPLE-INSPIRED PREMIUM DROPDOWN
   Ultra Modern • Liquid Gold • Glass Morphism
========================================================= */

@property --x1{
    syntax:"<percentage>";
    inherits:false;
    initial-value:20%;
}
@property --y1{
    syntax:"<percentage>";
    inherits:false;
    initial-value:30%;
}
@property --x2{
    syntax:"<percentage>";
    inherits:false;
    initial-value:80%;
}
@property --y2{
    syntax:"<percentage>";
    inherits:false;
    initial-value:70%;
}
@property --rotate{
    syntax:"<angle>";
    inherits:false;
    initial-value:0deg;
}

*{
    box-sizing:border-box;
}


/* =========================================================
   ANIMATIONS
========================================================= */

@keyframes liquidFlow{

    0%{
        --x1:18%;
        --y1:30%;
        --x2:82%;
        --y2:72%;
    }

    25%{
        --x1:72%;
        --y1:18%;
        --x2:25%;
        --y2:82%;
    }

    50%{
        --x1:88%;
        --y1:72%;
        --x2:18%;
        --y2:28%;
    }

    75%{
        --x1:35%;
        --y1:88%;
        --x2:82%;
        --y2:15%;
    }

    100%{
        --x1:18%;
        --y1:30%;
        --x2:82%;
        --y2:72%;
    }
}

@keyframes rotateGlow{
    from{
        --rotate:0deg;
    }
    to{
        --rotate:360deg;
    }
}

@keyframes reveal{
    from{
        opacity:0;
        transform:translateY(25px) scale(.96);
        filter:blur(15px);
    }
    to{
        opacity:1;
        transform:none;
        filter:none;
    }
}

@keyframes pulse{
    0%,100%{
        transform:translateY(-50%) scale(1);
    }
    50%{
        transform:translateY(-50%) scale(1.08);
    }
}

/* =========================================================
   DROPDOWN
========================================================= */

.dropdown{
    width:60%;
    max-width:650px;

    position:relative;

    margin:0 auto;   /* זה מה שחסר לך */

    animation:reveal .9s cubic-bezier(.22,1,.36,1);
}

/* =========================================================
   SEARCH BOX
========================================================= */

.search-box{
    position:relative;
    overflow:hidden;
    border-radius:34px;
}

/* Liquid Background */

.search-box::before{
    content:"";
    position:absolute;
    inset:0;

    background:

    radial-gradient(
        circle at var(--x1) var(--y1),
        rgba(255,198,82,.75),
        transparent 22%
    ),

    radial-gradient(
        circle at var(--x2) var(--y2),
        rgba(255,226,165,.85),
        transparent 24%
    ),

    radial-gradient(
        circle at 50% 50%,
        rgba(255,255,255,.85),
        transparent 70%
    ),

    linear-gradient(
        135deg,
        rgba(255,255,255,.96),
        rgba(248,248,250,.96)
    );

    animation:
    liquidFlow 14s ease-in-out infinite;

    z-index:0;
}

/* Premium Border */

.search-box::after{
    content:"";
    position:absolute;
    inset:-1px;
    border-radius:35px;

    background:
    conic-gradient(
        from var(--rotate),
        rgba(255,212,122,.8),
        rgba(255,255,255,.8),
        rgba(255,184,77,.8),
        rgba(255,255,255,.8),
        rgba(255,212,122,.8)
    );

    animation:
    rotateGlow 12s linear infinite;

    z-index:-1;

    filter:
    blur(14px);

    opacity:.85;
}

/* =========================================================
   INPUT
========================================================= */

.dropdown-search{

    position:relative;
    z-index:2;

    width:100%;

    padding:
    22px
    65px
    22px
    28px;

    border:none;
    outline:none;

    border-radius:34px;

    font-size:15px;
    font-weight:200;
    letter-spacing:.3px;

    color:#222;

    backdrop-filter:
    blur(18px);

    background:
    rgba(255,255,255,.45);

    box-shadow:

    inset 0 1px 0 rgba(255,255,255,.9),

    inset 0 -1px 0 rgba(255,255,255,.5),

    0 20px 40px rgba(0,0,0,.08),

    0 5px 15px rgba(0,0,0,.04);

    transition:
    .45s cubic-bezier(.22,1,.36,1);
}

.dropdown-search::placeholder{
    color:#9d9d9d;
}

.dropdown-search:focus{

    transform:
    translateY(-2px);

    box-shadow:

    inset 0 1px 0 rgba(255,255,255,1),

    0 28px 60px rgba(255,191,73,.18),

    0 8px 25px rgba(0,0,0,.08);
}

/* =========================================================
   CLEAR BUTTON
========================================================= */

.clear-btn{

    position:absolute;

    right:14px;
    top:50%;

    transform:
    translateY(-50%);

    width:42px;
    height:42px;

    border-radius:50%;

    display:flex;
    align-items:center;
    justify-content:center;

    z-index:3;

    cursor:pointer;

    font-size:24px;
    font-weight:300;

    color:#8b6a2f;

    background:
    linear-gradient(
        145deg,
        rgba(255,255,255,.95),
        rgba(248,245,236,.95)
    );

    backdrop-filter:
    blur(20px);

    box-shadow:

    inset 0 1px 0 rgba(255,255,255,.95),

    0 8px 20px rgba(0,0,0,.08),

    0 0 20px rgba(255,196,75,.15);

    transition:
    .35s cubic-bezier(.22,1,.36,1);

    animation:
    pulse 4s ease-in-out infinite;
}

.clear-btn:hover{

    transform:
    translateY(-50%)
    scale(1.12);

    box-shadow:

    0 12px 30px rgba(255,183,56,.25),

    0 0 30px rgba(255,211,122,.45);

    color:#c48a18;
}

/* =========================================================
   LIST
========================================================= */

.dropdown-list{

    margin-top:18px;
    overflow-y: auto;
    max-height:360px;
    border-radius:30px;
    display:none;

    backdrop-filter:
    blur(30px);

    background:

    linear-gradient(
        180deg,
        rgba(255,255,255,.92),
        rgba(248,248,249,.92)
    );

    border:
    1px solid rgba(255,255,255,.85);

    box-shadow:

    0 35px 70px rgba(0,0,0,.08),

    0 10px 25px rgba(0,0,0,.05),

    inset 0 1px 0 rgba(255,255,255,.9);

    animation:
    reveal .8s ease;
}

.dropdown.open .dropdown-list{
    display:block;
}


/* =========================================================
   ITEMS
========================================================= */

.dropdown-item{

    position:relative;

    padding:
    18px
    26px;

    font-size:14px;
    font-weight:300;

    letter-spacing:.25px;

    color:#2a2a2a;

    cursor:pointer;

    transition:
    .45s cubic-bezier(.22,1,.36,1);

    border-bottom:
    1px solid rgba(0,0,0,.04);

    overflow:hidden;
}

.dropdown-item:last-child{
    border-bottom:none;
}

.dropdown-item::before{

    content:"";

    position:absolute;

    left:-120%;
    top:0;

    width:100%;
    height:100%;

    background:
    linear-gradient(
        90deg,
        transparent,
        rgba(255,208,102,.45),
        transparent
    );

    transition:
    .75s ease;
}

.dropdown-item::after{

    content:"";

    position:absolute;

    left:18px;
    top:50%;

    width:8px;
    height:8px;

    border-radius:50%;

    background:
    linear-gradient(
        135deg,
        #ffd677,
        #d89b2d
    );

    transform:
    translateY(-50%)
    scale(0);

    transition:
    .35s ease;
}

.dropdown-item:hover{

    padding-left:42px;

    background:

    linear-gradient(
        90deg,
        rgba(255,218,127,.12),
        rgba(255,255,255,.35)
    );

    color:#111;

    transform:
    translateX(4px);
}

.dropdown-item:hover::before{
    left:120%;
}

.dropdown-item:hover::after{
    transform:
    translateY(-50%)
    scale(1);
}

/* =========================================================
   SOFT GOLD / WHITE PREMIUM SCROLLBAR
========================================================= */

.dropdown-list::-webkit-scrollbar{
    width:9px;
}

.dropdown-list::-webkit-scrollbar-track{
    background:transparent;
}

.dropdown-list::-webkit-scrollbar-thumb{

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,.95),
            rgba(255,236,180,.85),
            rgba(255,215,140,.75),
            rgba(255,248,220,.9)
        );

    border:2px solid rgba(255,255,255,.65);
    background-clip:padding-box;

    box-shadow:
        0 2px 10px rgba(0,0,0,.06),
        inset 0 1px 1px rgba(255,255,255,.9);

    transition:all .25s ease;
}

.dropdown-list:hover::-webkit-scrollbar-thumb{
    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,1),
            rgba(255,230,170,.95),
            rgba(255,210,120,.85),
            rgba(255,255,255,.95)
        );

    box-shadow:
        0 4px 16px rgba(0,0,0,.10),
        0 0 18px rgba(255,215,140,.25),
        inset 0 1px 1px rgba(255,255,255,1);
}

.dropdown-list::-webkit-scrollbar-thumb:hover{
    background:
        linear-gradient(
            180deg,
            #ffffff,
            #ffe7b0,
            #ffd77a,
            #fff6dc
        );

    box-shadow:
        0 6px 20px rgba(0,0,0,.12),
        0 0 22px rgba(255,215,140,.35);
}

/* Firefox support */
.dropdown-list{
    scrollbar-width:thin;
    scrollbar-color:rgba(255,215,140,.7) transparent;
}

/* Firefox */


/* =========================================================
   SOFT BREATHING GLOW
========================================================= */


/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width:768px){

    .dropdown-search{
        padding:19px 60px 19px 22px;
    }

    .dropdown-item{
        padding:16px 22px;
    }
}

/* ================= SUB FILTER ================= */
#subDropdown {
    margin-top: 16px;

    background: rgba(255,255,255,0.88);
    border-radius: 20px;

    padding: 18px;

    backdrop-filter: blur(14px);

    border: 4px solid rgba(0,0,0,0.04);

    box-shadow:
        0 20px 50px rgba(0,0,0,0.07),
        0 1px 0 rgba(255,255,255,0.7) inset;
}

/* ================= FILTER GROUP ================= */
.filter-group {
    margin-bottom: 18px;
}

.filter-title {
    font-weight: 600;
    margin-bottom: 10px;

    font-size: 12px;
    letter-spacing: 0.6px;
    text-transform: uppercase;

    color: #6b7280;
}

/* ================= OPTIONS ================= */
.filter-options {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

/* pills */
.sub-item {
    padding: 9px 14px;

    background: #f1f5f9;

    border-radius: 999px;

    cursor: pointer;

    font-size: 13px;
    font-weight: 500;

    color: #334155;

    border: 1px solid rgba(0,0,0,0.05);

    transition: all 0.18s ease;
}

.sub-item:hover {
    background: #eaf6ff;

    transform: translateY(-1px);

    box-shadow:
        0 6px 12px rgba(0,0,0,0.06);
}

.sub-item.selected {
    background: linear-gradient(90deg, #0072ff, #00c6ff);
    color: #fff;

    border-color: transparent;

    box-shadow:
        0 10px 22px rgba(0,114,255,0.25);
}

/* ================= SUB SEARCH ================= */
.sub-search {
    width: 40%;
    padding: 10px 14px;

    margin: 12px 0;

    border-radius: 12px;

    border: 1px solid rgba(0,0,0,0.06);

    font-size: 13px;

    background: rgba(255,255,255,0.9);

    transition: all 0.18s ease;
}

.sub-search:focus {
    border-color: rgba(0,114,255,0.35);

    box-shadow:
        0 0 0 3px rgba(0,114,255,0.10);
}

/* ================= SHOW MORE ================= */
.show-more {
    margin-top: 12px;

    font-size: 12px;
    font-weight: 500;

    color: #6b7280;

    cursor: pointer;

    display: inline-flex;
    align-items: center;
    gap: 4px;

    transition: all 0.18s ease;
}

.show-more:hover {
    color: #111827;
    transform: translateX(3px);
}

/* ================= ANIMATION ================= */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(6px) scale(0.98);
    }
    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}
.price-range {
    position: relative;
    width: 100%;
    padding: 50px 8px 10px;
    box-sizing: border-box;
    top: 100px;
    display: flex;
    flex-direction: column;
    gap: 22px;
    background: transparent;
}

.price-field {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 100px;
}

.price-field label {
    font-size: 12px;
    font-weight: 600;
    color: #6b7280;
    padding-left: 2px;
}

.price-range::before {
    content: "";
    position: absolute;
    left: 8px;
    right: 8px;
    top: 22px;
    height: 2px;
    border-radius: 999px;
    z-index: 1;

    background: linear-gradient(180deg, #effcff 0%, #8eeaff 100%);

    box-shadow:
        inset 0 1px 2px rgba(0,0,0,0.10),
        inset 0 -1px 2px rgba(255,255,255,0.7);
}

.price-range::after {
    content: "";
    position: absolute;
    left: calc(8px + var(--min) - 5px);
    right: calc(100% - var(--max));
    top: 22px;
    height: 2px;
    border-radius: 999px;
    z-index: 2;

    background: linear-gradient(90deg, #8eeaff 0%, #54dbff 50%, #38cfff 100%);

    box-shadow:
        inset 0 1px 1px rgba(255,255,255,0.35),
        inset 0 -1px 2px rgba(0,0,0,0.12),
        0 2px 6px rgba(120,240,255,0.35);
}
/* ================= FLOATING DOT WAVES ================= */

.bar{
    position: absolute;

    /* התחלה */
    left: calc(4px + var(--min) - 1px);
    top: -21px;

    /* סוף */
    right: calc(100% - var(--max));

    height: 150px;

    overflow: hidden;

    

    
}
.bar::before{
    content: "";

    position: absolute;

    inset: 0;

    border-radius: inherit;

    pointer-events: none;

    box-shadow:
        inset 0 0 22px rgba(255,255,255,.15),
        0 0 24px rgba(0,180,255,.12);

    z-index: 4;
}

/* =========================================
   INNER
========================================= */
.bar-inner{
    position: absolute;
    inset: 0;
    top:-5px ;
    overflow: hidden;
    pointer-events: none;

    z-index: 2;

    border-radius: inherit;
    
    height: 50px;
}

/* =========================================
   WAVE
========================================= */
.wave{
    position:absolute;

    left:0;
    top:20px;

    width:100%;
    height:36px;

    transform:translateY(-50%);
    transform-origin:center center;

    overflow:hidden;

    pointer-events:none;
    isolation:isolate;

    opacity:.98;

    will-change:
        transform,
        filter,
        clip-path;

    filter:
        blur(.12px)
        saturate(1.35)
        brightness(1.04);

    clip-path:
        polygon(
            0% 58%,
            4% 52%,
            8% 64%,
            12% 48%,
            16% 61%,
            20% 45%,
            24% 66%,
            28% 54%,
            32% 62%,
            36% 46%,
            40% 57%,
            44% 51%,
            48% 68%,
            52% 48%,
            56% 60%,
            60% 45%,
            64% 67%,
            68% 54%,
            72% 62%,
            76% 47%,
            80% 58%,
            84% 52%,
            88% 66%,
            92% 54%,
            96% 61%,
            100% 50%,
            100% 100%,
            0% 100%
        );

    animation:
        googleSurface 5s ease-in-out infinite;
}

/* =========================================
   MAIN GOOGLE LIQUID
========================================= */
.wave::before{
    content:"";

    position:absolute;
    inset:-25%;

    background:
        linear-gradient(
            90deg,

            #4285F4 0%,
            #6EA8FF 8%,
            #8FC2FF 14%,

            #34A853 24%,
            #7BDC6B 33%,

            #FBBC05 46%,
            #FFE27A 56%,

            #EA4335 68%,
            #FF9B91 78%,

            #4285F4 90%,
            #8FC2FF 100%
        );

    background-size:400% 100%;

    filter:
        blur(.2px)
        saturate(1.3);

    box-shadow:
        0 0 10px rgba(66,133,244,.20),
        0 0 20px rgba(66,133,244,.18),
        0 0 35px rgba(52,168,83,.16),
        0 0 55px rgba(251,188,5,.14),
        0 0 85px rgba(234,67,53,.10),

        inset 0 2px 5px rgba(255,255,255,.35),
        inset 0 -4px 10px rgba(66,133,244,.12);

    animation:
        googleFlow 4s linear infinite,
        googleBreath 3.5s ease-in-out infinite;
}

/* =========================================
   PREMIUM GLASS SHINE
========================================= */
.wave::after{
    content:"";

    position:absolute;
    inset:0;

    background:

        radial-gradient(
            circle at 8% 35%,
            rgba(255,255,255,.95),
            transparent 10%
        ),

        radial-gradient(
            circle at 28% 45%,
            rgba(255,255,255,.55),
            transparent 18%
        ),

        radial-gradient(
            circle at 52% 40%,
            rgba(255,255,255,.45),
            transparent 22%
        ),

        radial-gradient(
            circle at 75% 42%,
            rgba(255,255,255,.55),
            transparent 18%
        ),

        radial-gradient(
            circle at 92% 35%,
            rgba(255,255,255,.95),
            transparent 10%
        ),

        linear-gradient(
            180deg,
            rgba(255,255,255,.42),
            transparent 35%
        ),

        linear-gradient(
            90deg,
            rgba(255,255,255,.95),
            rgba(255,255,255,.08),
            rgba(255,255,255,.95)
        );

    mix-blend-mode:screen;

    animation:
        googleShimmer 2.5s linear infinite,
        googleGlow 3s ease-in-out infinite;
}

/* =========================================
   COLOR FLOW
========================================= */
@keyframes googleFlow{

    0%{
        background-position:0% 0%;
    }

    100%{
        background-position:400% 0%;
    }
}

/* =========================================
   LIQUID SURFACE
========================================= */
@keyframes googleSurface{

    0%{
        clip-path:
            polygon(
                0% 56%,
                8% 64%,
                16% 53%,
                24% 67%,
                32% 47%,
                40% 58%,
                48% 66%,
                56% 53%,
                64% 64%,
                72% 49%,
                80% 59%,
                88% 65%,
                100% 54%,
                100% 100%,
                0% 100%
            );
    }

    25%{
        clip-path:
            polygon(
                0% 51%,
                8% 61%,
                16% 46%,
                24% 64%,
                32% 52%,
                40% 68%,
                48% 54%,
                56% 63%,
                64% 47%,
                72% 58%,
                80% 50%,
                88% 67%,
                100% 57%,
                100% 100%,
                0% 100%
            );
    }

    50%{
        clip-path:
            polygon(
                0% 63%,
                8% 49%,
                16% 66%,
                24% 52%,
                32% 61%,
                40% 45%,
                48% 68%,
                56% 51%,
                64% 63%,
                72% 47%,
                80% 65%,
                88% 53%,
                100% 60%,
                100% 100%,
                0% 100%
            );
    }

    75%{
        clip-path:
            polygon(
                0% 54%,
                8% 66%,
                16% 50%,
                24% 61%,
                32% 44%,
                40% 65%,
                48% 53%,
                56% 64%,
                64% 48%,
                72% 57%,
                80% 50%,
                88% 66%,
                100% 55%,
                100% 100%,
                0% 100%
            );
    }

    100%{
        clip-path:
            polygon(
                0% 56%,
                8% 64%,
                16% 53%,
                24% 67%,
                32% 47%,
                40% 58%,
                48% 66%,
                56% 53%,
                64% 64%,
                72% 49%,
                80% 59%,
                88% 65%,
                100% 54%,
                100% 100%,
                0% 100%
            );
    }
}

/* =========================================
   SOFT GOOGLE BREATH
========================================= */
@keyframes googleBreath{

    0%{
        transform:
            scaleY(.93)
            scaleX(1);
    }

    50%{
        transform:
            scaleY(1.10)
            scaleX(1.015);
    }

    100%{
        transform:
            scaleY(.93)
            scaleX(1);
    }
}

/* =========================================
   LIGHT SWEEP
========================================= */
@keyframes googleShimmer{

    0%{
        transform:
            translateX(-100px);
    }

    100%{
        transform:
            translateX(100px);
    }
}

/* =========================================
   PREMIUM GLOW
========================================= */
@keyframes googleGlow{

    0%{
        opacity:.65;
        filter:blur(0px);
    }

    50%{
        opacity:1;
        filter:blur(.25px);
    }

    100%{
        opacity:.65;
        filter:blur(0px);
    }
}
.min-slider,
.max-slider{
    width:100%;
    position:absolute;
    left:0;
    top:-18px;
    transform:translateY(-40%);
    pointer-events:none;
    -webkit-appearance:none;
    appearance:none;
    background:transparent;
    z-index:30;
}

/* ==================================================
   MINIMAL GOOGLE / APPLE STYLE
   ================================================== */

.min-slider::-webkit-slider-thumb,
.max-slider::-webkit-slider-thumb{

    pointer-events:all;

    -webkit-appearance:none;
    appearance:none;

    width:8px;
    height:88px;

    cursor:pointer;
    border:none;

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #f5f7fa 18%,
            #e8edf3 45%,
            #dce4ec 100%
        );

    box-shadow:
        0 2px 6px rgba(0,0,0,.08),
        0 8px 18px rgba(0,0,0,.10),
        inset 0 1px 0 rgba(255,255,255,.95);

    transition:
        transform .18s ease,
        box-shadow .18s ease;
}

/* subtle blue center line */

.min-slider::-webkit-slider-thumb::before,
.max-slider::-webkit-slider-thumb::before{

    content:"";

    position:absolute;

    left:50%;
    top:50%;

    width:2px;
    height:34px;

    transform:translate(-50%,-50%);

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #8ab4f8,
            #4285f4
        );

    opacity:.9;
}

/* hover */

.min-slider::-webkit-slider-thumb:hover,
.max-slider::-webkit-slider-thumb:hover{

    transform:scaleX(1.08);

    box-shadow:
        0 3px 8px rgba(0,0,0,.10),
        0 10px 22px rgba(0,0,0,.12),
        inset 0 1px 0 rgba(255,255,255,1);
}

/* drag */

.min-slider::-webkit-slider-thumb:active,
.max-slider::-webkit-slider-thumb:active{

    transform:scaleX(1.15);

    box-shadow:
        0 2px 5px rgba(0,0,0,.10),
        0 6px 14px rgba(0,0,0,.12),
        inset 0 1px 0 rgba(255,255,255,1);
}

/* Firefox */

.min-slider::-moz-range-thumb,
.max-slider::-moz-range-thumb{

    width:8px;
    height:88px;

    border:none;
    cursor:pointer;

    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #f5f7fa 18%,
            #e8edf3 45%,
            #dce4ec 100%
        );

    box-shadow:
        0 2px 6px rgba(0,0,0,.08),
        0 8px 18px rgba(0,0,0,.10);
}
/* =========================================
   PRICE VALUES
========================================= */
/* =========================================
   PRICE VALUES
========================================= */
.price-values{
    display:flex;
    justify-content:space-between;
    align-items:flex-start;
    gap:12px;
    margin-top: 80px;
    margin-bottom: 200px;
    
}

.price-values .value-box{
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:6px;
    flex:1;
    margin-top:40px;
}

.price-values .value-box span{
    font-size:10px;
    font-weight:700;
    letter-spacing:1px;
    text-transform:uppercase;
    color:#7a8ca5;
}

/* =========================================================
   PREMIUM INPUT FIELD (PRICE VALUES)
   Clean Apple-like UI • Soft Depth • Subtle Glow • Smooth Feel
========================================================= */

.price-values input{

    width:100%;
    max-width:90px;
    height:75px;

    padding:0 12px;

    border:none;
    outline:none;

    border-radius:16px;

    background:
        linear-gradient(145deg, #ffffff, #f6fbff);

    text-align:center;

    font-size:18px;
    font-weight:500;
    letter-spacing:0.3px;

    color:#00a8e8;

    box-shadow:
        inset 0 1px 0 rgba(255,255,255,0.9),
        0 10px 20px rgba(0,0,0,0.05),
        0 0 0 1px rgba(0,181,241,0.10);

    transition:
        transform 0.22s cubic-bezier(.2,.9,.2,1),
        box-shadow 0.22s ease,
        background 0.22s ease,
        color 0.22s ease;
}

/* hover floating */
.price-values input:hover{

    transform:
        translateY(-2px) scale(1.03);

    box-shadow:
        0 14px 28px rgba(0,0,0,0.08),
        0 0 0 1px rgba(0,181,241,0.18);
}

/* focus glow */
.price-values input:focus{

    color:#007fd1;

    background:
        linear-gradient(145deg, #ffffff, #eef9ff);

    box-shadow:
        0 0 0 4px rgba(0,181,241,0.12),
        0 18px 40px rgba(0,181,241,0.15);
}

/* =========================================
   CURRENCY
========================================= */
.currency-box{
    position:relative;
    display:flex;
    flex-direction:column;

    top:-320px;
    margin:-160px auto 10px;

    isolation:isolate;
}

/* ==================================================
   HEADER
================================================== */

.currency-header{
    padding:18px 20px;

    border-radius:24px;

    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,.95),
            rgba(245,251,255,.82)
        );

    backdrop-filter:blur(24px);
    -webkit-backdrop-filter:blur(24px);

    cursor:pointer;

    border:1px solid rgba(255,255,255,.65);

    box-shadow:
        0 20px 60px rgba(0,0,0,.08),
        0 8px 20px rgba(0,181,241,.08),
        inset 0 1px 0 rgba(255,255,255,.9),
        inset 0 -1px 0 rgba(255,255,255,.4);

    transition:
        transform .35s cubic-bezier(.22,1,.36,1),
        box-shadow .35s cubic-bezier(.22,1,.36,1),
        border-color .35s ease;

    position:relative;
    overflow:hidden;
    
    
}
.currency-box,
.currency-box *{
    font-family:
    "Space Grotesk",
    "Satoshi",
    "Manrope",
    "Inter",
    "SF Pro Display",
    sans-serif;
    font-weight: 300;
        font-size:18px;
}
/* glow layer */

.currency-header::before{
    content:"";

    position:absolute;
    inset:-40%;

    background:
        radial-gradient(
            circle,
            rgba(0,198,255,.22),
            transparent 70%
        );

    opacity:.4;

    animation:
        headerGlow 6s linear infinite;

    pointer-events:none;
}

/* premium shine */

.currency-header::after{
    content:"";

    position:absolute;
    inset:0;

    background:
        linear-gradient(
            115deg,
            transparent 20%,
            rgba(255,255,255,.08) 35%,
            rgba(255,255,255,.85) 50%,
            rgba(255,255,255,.08) 65%,
            transparent 80%
        );

    transform:translateX(-160%);
    animation:headerShine 4.5s ease-in-out infinite;
}

@keyframes headerShine{
    0%{
        transform:translateX(-160%);
    }

    35%{
        transform:translateX(180%);
    }

    100%{
        transform:translateX(180%);
    }
}

@keyframes headerGlow{
    0%{
        transform:
            rotate(0deg)
            scale(1);
    }

    50%{
        transform:
            rotate(180deg)
            scale(1.15);
    }

    100%{
        transform:
            rotate(360deg)
            scale(1);
    }
}

.currency-header:hover{
    transform:
        translateY(-4px)
        scale(1.015);

    border-color:
        rgba(0,181,241,.25);

    box-shadow:
        0 30px 80px rgba(0,181,241,.14),
        0 15px 35px rgba(0,0,0,.08),
        inset 0 1px 0 rgba(255,255,255,.95);
}

/* OPEN STATE */

.currency-box.open .currency-header{
    box-shadow:
        0 40px 100px rgba(0,181,241,.18),
        0 0 0 1px rgba(0,181,241,.18),
        inset 0 1px 0 rgba(255,255,255,.9);

    animation:
        headerPulse 2.4s ease-in-out infinite;
}

@keyframes headerPulse{
    0%,100%{
        transform:translateY(0);
    }

    20%{
        transform:translateY(-2px);
    }
}

/* ==================================================
   DROPDOWN
================================================== */

.currency-dropdown{
    display:none;

    position:absolute;
    top:calc(100% + 16px);

    width:180px;

    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,.94),
            rgba(247,252,255,.90)
        );

    backdrop-filter:blur(24px);

    border-radius:26px;

    padding:18px;

    z-index:50;

    border:
        1px solid rgba(255,255,255,.8);

    box-shadow:
        0 40px 120px rgba(0,0,0,.12),
        0 15px 35px rgba(0,181,241,.08);

    overflow:hidden;
}

.currency-dropdown::before{
    content:"";

    position:absolute;
    inset:0;

    background:
        linear-gradient(
            135deg,
            rgba(255,255,255,.35),
            transparent 40%
        );

    pointer-events:none;
}

.currency-box.open .currency-dropdown{
    display:block;

    animation:
        dropdownFloat .35s cubic-bezier(.22,1,.36,1);
}

@keyframes dropdownFloat{
    from{
        opacity:0;

        transform:
            translateY(-14px)
            scale(.96);
    }

    to{
        opacity:1;

        transform:
            translateY(0)
            scale(1);
    }
}

/* ==================================================
   SEARCH
================================================== */

.currency-search{
    width:100%;

    padding:14px 16px;

    border:none;
    outline:none;

    border-radius:18px;

    background:
        rgba(255,255,255,.85);

    box-shadow:
        inset 0 0 0 1px rgba(0,0,0,.05);

    margin-bottom:14px;

    transition:
        .28s cubic-bezier(.22,1,.36,1);
}

.currency-search::placeholder{
    color:#8aa2b8;
}

.currency-search:focus{
    background:#fff;

    transform:
        translateY(-2px)
        scale(1.01);

    box-shadow:
        0 0 0 4px rgba(0,181,241,.12),
        0 14px 35px rgba(0,181,241,.12);
}

/* ==================================================
   LIST
================================================== */

.currency-list{
    max-height:260px;
    overflow-y:auto;

    padding-right:4px;
}

.currency-list::-webkit-scrollbar{
    width:8px;
}

.currency-list::-webkit-scrollbar-track{
    background:transparent;
}

.currency-list::-webkit-scrollbar-thumb{
    border-radius:999px;

    background:
        linear-gradient(
            180deg,
            #d9e5f0,
            #89dfff
        );
}

/* ==================================================
   ITEM
================================================== */

.currency-item{
    position:relative;

    padding:14px 14px;

    border-radius:16px;

    cursor:pointer;

    overflow:hidden;

    transition:
        transform .25s cubic-bezier(.22,1,.36,1),
        box-shadow .25s cubic-bezier(.22,1,.36,1),
        background .25s ease;
}

.currency-item::before{
    content:"";

    position:absolute;
    inset:0;

    background:
        linear-gradient(
            120deg,
            transparent,
            rgba(255,255,255,.7),
            transparent
        );

    transform:translateX(-140%);
    transition:transform .55s ease;
}

.currency-item:hover{
    background:
        linear-gradient(
            135deg,
            #f8fdff,
            #e7f8ff
        );

    transform:
        translateX(6px)
        scale(1.015);

    box-shadow:
        0 12px 28px rgba(0,181,241,.10);
}

.currency-item:hover::before{
    transform:translateX(140%);
}

.currency-item.active{
    color:#fff;

    background:
        linear-gradient(
            135deg,
            #009dff,
            #00d4ff,
            #5ce7ff
        );

    background-size:250% 250%;

    animation:
        activeFlow 5s linear infinite;

    box-shadow:
        0 18px 40px rgba(0,181,241,.28),
        0 0 25px rgba(0,212,255,.18);

    font-weight:700;
}

@keyframes activeFlow{
    0%{
        background-position:0% 50%;
    }

    100%{
        background-position:250% 50%;
    }
}
/* =========================================
   CATEGORIES
========================================= */
.categories{

    display:flex;
    flex-wrap:wrap;

    justify-content:center;

    gap:14px;

    padding:16px;
}

.circle{

    width:74px;
    height:74px;

    overflow:hidden;

    border-radius:50%;

    background:#fff;

    box-shadow:
        0 4px 14px rgba(0,0,0,.05);

    transition:
        .18s ease;
}

.category:hover .circle{

    transform:
        scale(1.06);

    box-shadow:
        0 8px 20px rgba(0,0,0,.08);
}

.category.active .circle{

    transform:
        scale(1.06);

    box-shadow:
        0 0 0 3px rgba(0,181,241,.18),
        0 8px 20px rgba(0,181,241,.15);
}

.category.hidden{

    opacity:0;

    transform:
        scale(.85);

    pointer-events:none;

    position:absolute;
}

/* =========================================
   FOOTER
========================================= */
.bottom-footer{

    margin-top:50px;

    padding:16px;

    display:flex;
    justify-content:center;

    gap:20px;

    font-size:12px;
}

.bottom-footer a{

    color:#7a8ca5;

    text-decoration:none;

    transition:.15s ease;
}

.bottom-footer a:hover{
    color:#00b5f1;
}

/* =========================================
   FLOAT BUTTON
========================================= */
#floatingSearchBtn{

    position:fixed;

    bottom:20px;
    right:20px;

    padding:14px 24px;

    border:none;

    border-radius:16px;

    background:
        linear-gradient(
            135deg,
            #00c2ff,
            #00a8f5
        );

    color:white;

    font-size:14px;
    font-weight:700;

    cursor:pointer;

    box-shadow:
        0 10px 30px rgba(0,181,241,.22);

    opacity:0;
    visibility:hidden;

    transform:
        translateY(15px);

    transition:
        .22s ease;
}

#floatingSearchBtn.show{

    opacity:1;
    visibility:visible;

    transform:
        translateY(0);
}

#floatingSearchBtn:hover{

    transform:
        translateY(-2px);

    box-shadow:
        0 14px 35px rgba(0,181,241,.28);
}
/* =========================================
   ANIMATIONS
========================================= */
@keyframes paintBlob{

    0%{
        border-radius:
            52% 48% 58% 42% /
            42% 57% 43% 58%;
    }

    25%{
        border-radius:
            63% 37% 46% 54% /
            54% 42% 58% 46%;
    }

    50%{
        border-radius:
            40% 60% 52% 48% /
            61% 38% 62% 39%;
    }

    75%{
        border-radius:
            57% 43% 63% 37% /
            44% 58% 42% 56%;
    }

    100%{
        border-radius:
            52% 48% 58% 42% /
            42% 57% 43% 58%;
    }
}

@keyframes brushTextFloat{

    0%,100%{
        transform:translateY(0);
    }

    50%{
        transform:translateY(-2px);
    }
}

@keyframes dropPaint{

    from{
        opacity:0;
        transform:
            translateY(-12px)
            scale(.95);
    }

    to{
        opacity:1;
        transform:
            translateY(0)
            scale(1);
    }
}
